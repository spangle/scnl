<h1>Create Driver</h1>

<%= form_for(@driver) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'fields', :f => f %>
  <div class="actions">
    <%= f.submit "Create Driver" %>
  </div>  
<% end %>
